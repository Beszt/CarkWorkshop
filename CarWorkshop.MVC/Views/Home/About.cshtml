@model About

@{
    ViewData["Title"] = @Model.Tittle;
}

<h1>@Model.Tittle</h1>
<h2>@Model.Description</h2>
<p>Tags:</p>
<ul>
    @foreach (var tag in Model.Tags)
    {
        <li>@tag</li>
    }
</ul>